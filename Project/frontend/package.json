{
  "name": "disease-prediction-frontend",
  "version": "1.0.0",
  "description": "Frontend for Disease Prediction Application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint script.js",
    "lint:fix": "eslint script.js --fix"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "html-validate": "^8.18.0",
    "stylelint": "^16.5.0",
    "stylelint-config-standard": "^36.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
